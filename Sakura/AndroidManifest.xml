<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="cn.ismartv.speedtester"
          android:installLocation="internalOnly"
          android:versionCode="101"
          android:versionName="1.01">
    <uses-sdk android:minSdkVersion="14"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <uses-permission android:name="android.permission.BLUETOOTH"></uses-permission>
    <uses-permission android:name="android.permission.RUN_INSTRUMENTATION"/>



    <instrumentation
            android:name="android.test.InstrumentationTestRunner"
            android:targetPackage="cn.ismartv.speedtester"
            android:label="Test for my app"
            />

    <application
            android:name=".SakuraApplication"
            android:label="@string/app_name"
            android:icon="@drawable/ic_launcher"
            android:theme="@android:style/Theme.Holo.NoActionBar.Fullscreen"
            >
        <uses-library android:name="android.test.runner"/>
        <meta-data android:name="AA_DB_NAME" android:value="sakura.db"/>
        <meta-data android:name="AA_DB_VERSION" android:value="2"/>

        <activity android:name=".ui.activity.MenuActivity"
                  android:launchMode="singleTask"
                  android:label="@string/app_name"
                >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <intent-filter>
                <action android:name="cn.ismartv.speedtester.feedback"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.HomeActivity"
                  android:launchMode="singleTask"
                >
        </activity>

        <activity
                android:name=".setting.OobeActivity"
                android:theme="@style/oobe_for_s52_style"
                >

            <intent-filter>
                <action android:name="cn.ismartv.speedtester.settings"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>


        <!--receiver-->
        <receiver android:name="com.ismartv.android.vod.receiver.BootBroadcastReceiver">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
        <receiver android:name="com.ismartv.android.vod.receiver.NetWorkBroadcastReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>

        <!--service-->
        <service android:name="com.ismartv.android.vod.service.HttpProxyService"></service>

        <!--content provider-->
        <provider android:authorities="cn.ismartv.speedtester"
                  android:exported="false"
                  android:name="com.activeandroid.content.ContentProvider"/>
    </application>


</manifest>
